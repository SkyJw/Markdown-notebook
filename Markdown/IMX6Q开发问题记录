#IMX6Q开发问题记录

###设备树相关

- 设备树添加节点后，应该将status设置为okay，使能节点
- 成功添加后，应该能在/sys/devices目录下找到节点
- of_node/目录是设备树节点目录，和设备节点不是同一个意思
- 填写compatible属性时，切记逗号后面不要有空格
- 设备树中添加节点时，要注意地址有无冲突
- mfgtool只烧录设备树文件，可以通过修改ucl文件实现
- ubuntu往盘拷贝文件时，有时候会失败。是没有umount导致的
